_schema-version: "3.2"
ID: athbmakranczlowcodeappopt
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: athbmakranczlowcodeappopt-destination-content
  type: com.sap.application.content
  requires:
  - name: athbmakranczlowcodeappopt-destination-service
    parameters:
      content-target: true
  - name: athbmakranczlowcodeappopt-repo-host
    parameters:
      service-key:
        name: athbmakranczlowcodeappopt-repo-host-key
  - name: athbmakranczlowcodeappopt-uaa
    parameters:
      service-key:
        name: athbmakranczlowcodeappopt-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: athbmakranczlowcodeappopt_html_repo_host
          ServiceInstanceName: athbmakranczlowcodeappopt-html5-service
          ServiceKeyName: athbmakranczlowcodeappopt-repo-host-key
          sap.cloud.service: athbmakranczlowcodeappopt
        - Authentication: OAuth2UserTokenExchange
          Name: athbmakranczlowcodeappopt_uaa
          ServiceInstanceName: athbmakranczlowcodeappopt-xsuaa-service
          ServiceKeyName: athbmakranczlowcodeappopt-uaa-key
          sap.cloud.service: athbmakranczlowcodeappopt
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: athbmakranczlowcodeappopt-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: athbmakranczlowcodeappopt-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - athbmakranczlowcodeappopt.zip
      name: athbmakranczlowcodeappopt
      target-path: resources/
- name: athbmakranczlowcodeappopt
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: athbmakranczlowcodeappopt-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: athbmakranczlowcodeappopt-destination-service
    service-plan: lite
- name: athbmakranczlowcodeappopt-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: athbmakranczlowcodeappopt-xsuaa-service
    service-plan: application
- name: athbmakranczlowcodeappopt-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: athbmakranczlowcodeappopt-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
